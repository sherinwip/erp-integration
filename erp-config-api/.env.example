# Same local Docker Postgres this repo's other services target -- see
# database/liquibase.properties and transformation-svc/.env.example.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=erp_integration
DB_USER=root
DB_PASSWORD=root
